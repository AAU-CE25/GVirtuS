nvidia-smi per-process accounting during the probe (dpu-02, two concurrent processes).
Identical with the MPS pipe directory reachable and unreachable, which is the point:
the per-client device footprint does not depend on MPS.

  563845, nvidia-cuda-mps-server, 28 MiB
  1432561, ./ctx_probe, 424 MiB
  1432562, ./ctx_probe, 424 MiB
