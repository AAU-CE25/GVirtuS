# requirements.txt

# Core
pyspark>=3.4.0
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=12.0.0

# For RAPIDS (install via conda from rapidsai channel)
# conda install -c rapidsai -c conda-forge -c nvidia rapids=24.10 python=3.10 cuda-version=12.0
# cudf
# cuml

# RAPIDS Spark plugin (download JAR separately)
# https://nvidia.github.io/spark-rapids/docs/download.html

# Benchmarking utilities
matplotlib>=3.7.0
seaborn>=0.12.0